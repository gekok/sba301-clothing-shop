/**
 * Spring Data JPA repositories.
 *
 * <p>Each entity gets a {@code XxxRepository extends JpaRepository<Xxx, Long>}.
 * Add custom query methods via Spring Data naming convention or {@code @Query}.
 */
package com.sba301.ecommerce.repository;
