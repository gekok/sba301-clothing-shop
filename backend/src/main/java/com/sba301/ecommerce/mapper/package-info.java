/**
 * Entity &harr; DTO mappers.
 *
 * <p>Implemented with MapStruct ({@code @Mapper(componentModel = "spring")})
 * so each mapper is a Spring bean and can be injected like any other component.
 */
package com.sba301.ecommerce.mapper;
