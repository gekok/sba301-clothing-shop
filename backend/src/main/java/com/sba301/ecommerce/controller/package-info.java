/**
 * REST API controllers.
 *
 * <p>Each controller is annotated with {@code @RestController} and
 * {@code @RequestMapping("/...")}. Receives DTO from {@code dto.request},
 * returns DTO from {@code dto.response}. NEVER expose entity directly.
 */
package com.sba301.ecommerce.controller;
